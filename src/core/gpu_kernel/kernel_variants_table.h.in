// AUTO-GENERATED by CMake from SCAMP_VARIANT_TUPLES in
// src/core/gpu_kernel/CMakeLists.txt. Do NOT edit by hand -- update the
// tuple list in CMakeLists.txt and re-run CMake; the embedded
// CMAKE_CONFIGURE_DEPENDS hint reruns configure when the CMakeLists.txt
// changes.
//
// Provides the kVariants[] table consumed by kernel_config.cpp. The
// variant index assigned to each row is its position in the tuple list
// (0, 1, 2, ...); reordering the list reorders the indices.
#pragma once

#include <array>

#include "kernel_config.h"

namespace SCAMP {

constexpr std::array<KernelVariantGeometry, @SCAMP_NUM_VARIANTS@> kVariants{{
@SCAMP_KVARIANTS_INIT@}};

}  // namespace SCAMP
